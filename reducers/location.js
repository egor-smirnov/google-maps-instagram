const initialState = {
    latitude: 55.751244,
    longitude: 37.618423
};

export function location(state = initialState, action) {
    switch (action.type) {
        
        default:
            return state;
    }
}